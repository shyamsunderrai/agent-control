/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  TimeseriesBucket,
  TimeseriesBucket$inboundSchema,
} from "./timeseries-bucket.js";

/**
 * Agent-level aggregate statistics.
 *
 * @remarks
 *
 * Invariant: execution_count = match_count + non_match_count + error_count
 *
 * Matches have actions (allow, deny, steer, warn, log) tracked in action_counts.
 * sum(action_counts.values()) == match_count
 *
 * Attributes:
 *     execution_count: Total executions across all controls
 *     match_count: Total matches across all controls (evaluator matched)
 *     non_match_count: Total non-matches across all controls (evaluator didn't match)
 *     error_count: Total errors across all controls (evaluation failed)
 *     action_counts: Breakdown of actions for matched executions
 *     timeseries: Time-series data points (only when include_timeseries=true)
 */
export type StatsTotals = {
  /**
   * Action breakdown for matches: {allow, deny, steer, warn, log}
   */
  actionCounts?: { [k: string]: number } | undefined;
  /**
   * Total errors
   */
  errorCount: number;
  /**
   * Total executions
   */
  executionCount: number;
  /**
   * Total matches
   */
  matchCount: number;
  /**
   * Total non-matches
   */
  nonMatchCount: number;
  /**
   * Time-series data points (only when include_timeseries=true)
   */
  timeseries?: Array<TimeseriesBucket> | null | undefined;
};

/** @internal */
export const StatsTotals$inboundSchema: z.ZodMiniType<StatsTotals, unknown> = z
  .pipe(
    z.object({
      action_counts: types.optional(z.record(z.string(), types.number())),
      error_count: z._default(types.number(), 0),
      execution_count: types.number(),
      match_count: z._default(types.number(), 0),
      non_match_count: z._default(types.number(), 0),
      timeseries: z.optional(
        z.nullable(z.array(TimeseriesBucket$inboundSchema)),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "action_counts": "actionCounts",
        "error_count": "errorCount",
        "execution_count": "executionCount",
        "match_count": "matchCount",
        "non_match_count": "nonMatchCount",
      });
    }),
  );

export function statsTotalsFromJSON(
  jsonString: string,
): SafeParseResult<StatsTotals, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StatsTotals$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StatsTotals' from JSON`,
  );
}
